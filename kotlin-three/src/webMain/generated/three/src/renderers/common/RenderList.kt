// Generated by Karakum - do not modify it manually!

@file:JsModule("threethree")

package three.src.renderers.common

// unhandled import: Camera from "../../cameras/Camera.js"
// unhandled import: BufferGeometry from "../../core/BufferGeometry.js"
// unhandled import: GeometryGroup from "../../core/BufferGeometry.js"
// unhandled import: Object3D from "../../core/Object3D.js"
// unhandled import: Light from "../../lights/Light.js"
// unhandled import: Material from "../../materials/Material.js"
// unhandled import: LightsNode from "../../nodes/Nodes.js"
// unhandled import: default as BundleGroup from "./BundleGroup.js"
// unhandled import: default as ClippingContext from "./ClippingContext.js"
// unhandled import: default as Lighting from "./Lighting.js"



















external interface Bundle {
var bundleGroup: BundleGroup
var camera: Camera
var renderList: RenderList
}

external interface RenderItem {
var id: Double?
var `object`: Object3D?
var geometry: BufferGeometry?
var material: Material?
var groupOrder: Double?
var renderOrder: Double?
var z: Double?
var group: GeometryGroup?
var clippingContext: ClippingContext?
}

/**
 * When the renderer analyzes the scene at the beginning of a render call,
 * it stores 3D object for further processing in render lists. Depending on the
 * properties of a 3D objects (like their transformation or material state), the
 * objects are maintained in ordered lists for the actual rendering.
 *
 * Render lists are unique per scene and camera combination.
 *
 * @private
 * @augments Pipeline
 */
external class RenderList {
/**
     * Constructs a render list.
     *
     * @param {Lighting} lighting - The lighting management component.
     * @param {Scene} scene - The scene.
     * @param {Camera} camera - The camera the scene is rendered with.
     */
constructor (lighting: Lighting, scene: Object3D, camera: Camera)
var renderItems: js.array.ReadonlyArray<RenderItem>
var renderItemsIndex: Double
var opaque: js.array.ReadonlyArray<RenderItem>
var transparentDoublePass: js.array.ReadonlyArray<RenderItem>
var transparent: js.array.ReadonlyArray<RenderItem>
var bundles: js.array.ReadonlyArray<Bundle>
var lightsNode: LightsNode
var lightsArray: js.array.ReadonlyArray<Light>
var scene: Object3D
var camera: Camera
var occlusionQueryCount: Double
/**
     * This method is called right at the beginning of a render call
     * before the scene is analyzed. It prepares the internal data
     * structures for the upcoming render lists generation.
     *
     * @return {RenderList} A reference to this render list.
     */
fun begin(): Unit /* this */
/**
     * Returns a render item for the giving render item state. The state is defined
     * by a series of object-related parameters.
     *
     * The method avoids object creation by holding render items and reusing them in
     * subsequent render calls (just with different property values).
     *
     * @param {Object3D} object - The 3D object.
     * @param {BufferGeometry} geometry - The 3D object's geometry.
     * @param {Material} material - The 3D object's material.
     * @param {number} groupOrder - The current group order.
     * @param {number} z - Th 3D object's depth value (z value in clip space).
     * @param {?number} group - {?Object} group - Only relevant for objects using multiple materials. This represents a group entry from the respective `BufferGeometry`.
     * @param {ClippingContext} clippingContext - The current clipping context.
     * @return {Object} The render item.
     */
fun getNextRenderItem(`object`: Object3D, geometry: BufferGeometry, material: Material, groupOrder: Double, z: Double, group: GeometryGroup?, clippingContext: ClippingContext?): RenderItem
/**
     * Pushes the given object as a render item to the internal render lists.
     * The selected lists depend on the object properties.
     *
     * @param {Object3D} object - The 3D object.
     * @param {BufferGeometry} geometry - The 3D object's geometry.
     * @param {Material} material - The 3D object's material.
     * @param {number} groupOrder - The current group order.
     * @param {number} z - Th 3D object's depth value (z value in clip space).
     * @param {?number} group - {?Object} group - Only relevant for objects using multiple materials. This represents a group entry from the respective `BufferGeometry`.
     * @param {ClippingContext} clippingContext - The current clipping context.
     */
fun push(`object`: Object3D, geometry: BufferGeometry, material: Material, groupOrder: Double, z: Double, group: GeometryGroup?, clippingContext: ClippingContext?): Unit
/**
     * Inserts the given object as a render item at the start of the internal render lists.
     * The selected lists depend on the object properties.
     *
     * @param {Object3D} object - The 3D object.
     * @param {BufferGeometry} geometry - The 3D object's geometry.
     * @param {Material} material - The 3D object's material.
     * @param {number} groupOrder - The current group order.
     * @param {number} z - Th 3D object's depth value (z value in clip space).
     * @param {?number} group - {?Object} group - Only relevant for objects using multiple materials. This represents a group entry from the respective `BufferGeometry`.
     * @param {ClippingContext} clippingContext - The current clipping context.
     */
fun unshift(`object`: Object3D, geometry: BufferGeometry, material: Material, groupOrder: Double, z: Double, group: GeometryGroup?, clippingContext: ClippingContext?): Unit
/**
     * Pushes render bundle group data into the render list.
     *
     * @param {Object} group - Bundle group data.
     */
fun pushBundle(group: Bundle): Unit
/**
     * Pushes a light into the render list.
     *
     * @param {Light} light - The light.
     */
fun pushLight(light: Light): Unit
/**
     * Sorts the internal render lists.
     *
     * @param {?function(any, any): number} customOpaqueSort - A custom sort function for opaque objects.
     * @param {?function(any, any): number} customTransparentSort -  A custom sort function for transparent objects.
     */
fun sort(customOpaqueSort: ((a: RenderItem, b: RenderItem) -> Double)?, customTransparentSort: ((a: RenderItem, b: RenderItem) -> Double)?): Unit
/**
     * This method performs finalizing tasks right after the render lists
     * have been generated.
     */
fun finish(): Unit
}

/* export default RenderList; */
